{% macro forecast_list(day, detail) %}
<li>
    <p class="date">{{ detail.date }}</p>
    <i id="{{ day }}_icon"></i>
    <p class="type">{{ detail.low }}/{{ detail.high }}</p>
    <p class="type" id="{{ day }}_type">{{ detail.type }}</p>
</li>
{% endmacro %}